create database jpatutorial;

use jpatutorial;

CREATE TABLE student (
                         id INT PRIMARY KEY,
                         name VARCHAR(255),
                         address VARCHAR(255)
);